---
import Layout from "@/layouts/Layout.astro";
import data from "@data";
---

<Layout title="Estrategias" description="Estrategias de Innova SA">
  <section
    class="bg-white relative dark:bg-gray-900 bg-[url('https://flowbite.s3.amazonaws.com/docs/jumbotron/hero-pattern.svg')] dark:bg-[url('https://flowbite.s3.amazonaws.com/docs/jumbotron/hero-pattern-dark.svg')]"
  >
    <div
      class="py-8 px-4 mx-auto max-w-screen-xl text-center lg:py-16 z-10 relative"
    >
      <h1
        class="mb-4 text-4xl font-extrabold tracking-tight leading-none text-gray-900 md:text-5xl lg:text-6xl dark:text-white"
      >
        Objetivos Estratégicos de TI
      </h1>
      <p
        class="mb-8 text-lg font-normal text-gray-500 lg:text-xl sm:px-16 lg:px-48 dark:text-gray-200"
      >
        Plan estratégico de tecnologías de información y comunicación para
        Innova SA
      </p>
    </div>
    <div
      class="bg-gradient-to-b from-blue-50 to-transparent dark:from-blue-900 w-full h-full absolute top-0 left-0 z-0"
    >
    </div>
  </section>

  <div class="px-4 mx-auto max-w-screen-xl py-8">
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
      {
        data.estrategias.objetivos.map((objetivo) => (
          <article class="m-4 p-6 bg-white rounded-lg shadow-lg transform hover:scale-105 transition-transform duration-300 border-l-4 border-gray-900 max-h-fit flex flex-col justify-between">
            <h3 class="mb-4 text-xl font-bold text-gray-900">{objetivo.title}</h3>
            <p class="mb-3 text-gray-600 dark:text-gray-700">{objetivo.content}</p>
            <div class="mt-4 space-y-3">
              <div>
                <h4 class="font-semibold text-gray-800 dark:text-gray-700">Estrategias:</h4>
                <p class="text-gray-600 dark:text-gray-700">{objetivo.estrategias}</p>
              </div>
              <div>
                <h4 class="font-semibold text-gray-800 dark:text-gray-700">Factores Críticos:</h4>
                <p class="text-gray-600 dark:text-gray-700">{objetivo.factores_criticos}</p>
              </div>
            </div>
          </article>
          <article class="m-4 p-6 bg-white rounded-lg shadow-lg transform hover:scale-105 transition-transform duration-300 border-l-4 border-gray-900">
            <h3 class="mb-4 text-xl font-bold text-orange-800 dark:text-gray-900">{objetivo.title}</h3>
            <p class="mb-3 text-gray-700 dark:text-gray-700">{objetivo.content}</p>
            <button 
              onclick={"document.getElementById('modal-" + objetivo.id + "').showModal()"}
              class="inline-flex items-center px-5 py-3 text-sm font-medium text-center text-white bg-gray-900 rounded-lg hover:bg-blue-800 focus:ring-4 focus:outline-none focus:ring-blue-300 dark:hover:bg-gray-700 dark:focus:ring-blue-800"
            >
              Ver más detalles
            </button>

            <dialog 
              id={"modal-" + objetivo.id}
              class="p-6 rounded-lg bg-white dark:bg-gray-800 backdrop:bg-black/50"
            >
              <div class="space-y-4">
                <div>
                  <h4 class="font-semibold text-gray-900">Estrategias:</h4>
                  <p class="text-gray-700">{objetivo.estrategias}</p>
                </div>
                <div>
                  <h4 class="font-semibold text-gray-900">Factores Críticos:</h4>
                  <p class="text-gray-700">{objetivo.factores_criticos}</p>
                </div>
                <button
                  onclick={"document.getElementById('modal-" + objetivo.id + "').close()"}
                  class="inline-flex items-center px-5 py-3 text-sm font-medium text-center text-white bg-gray-900 rounded-lg hover:bg-blue-800 focus:ring-4 focus:outline-none focus:ring-blue-300 dark:hover:bg-gray-700 dark:focus:ring-blue-800"
                >
                  Cerrar
                </button>
              </div>
            </dialog>
          </article>
        ))
      }
    </div>

    <div class="text-center mt-8">
      <a
        href="/"
        class="inline-flex items-center px-5 py-3 text-sm font-medium text-center text-white bg-gray-900 rounded-lg hover:bg-blue-800 focus:ring-4 focus:outline-none focus:ring-blue-300 dark:hover:bg-gray-700 dark:focus:ring-blue-800"
      >
        Volver a Inicio
      </a>
    </div>
  </div>
</Layout>
